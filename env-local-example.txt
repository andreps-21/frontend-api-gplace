# Copie para .env.local (ou use .env.example como base).
# Ligação ao api-gplace em Docker (porta 8005 por omissão).

NEXT_PUBLIC_API_URL=http://localhost:8005/api/v1
NEXT_PUBLIC_APP_TOKEN=gplace-local-frontend
NEXT_PUBLIC_APP_NAME=Gplace

# Opcional: dashboard + sidebar sem login (só dev).
# NEXT_PUBLIC_UI_PREVIEW=true

# O token gplace-local-frontend é criado na API por LocalDevStoreSeeder (db:seed com APP_ENV=local).
# Se tiveres .env.local com NEXT_PUBLIC_APP_TOKEN vazio, apaga a linha — senão o Next ignora o valor por omissão.
